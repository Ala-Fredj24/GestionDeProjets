# Namespace dédié à GestionDeProjets.
# Toutes les ressources du projet vivent dans ce namespace, ce qui les isole
# des autres applications éventuellement déployées sur le même cluster.
apiVersion: v1
kind: Namespace
metadata:
  name: gestiondeprojets
  labels:
    app: gestiondeprojets
