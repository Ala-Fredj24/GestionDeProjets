global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    env: docker-compose
    app: gestiondeprojets

scrape_configs:
  - job_name: gestiondeprojets-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - backend:8085
